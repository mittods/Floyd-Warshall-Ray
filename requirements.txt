# Dependencias principales
ray[default]==2.44.0
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0

# GPU (CUDA 12.x — compatible con driver NVIDIA 580.x / CUDA 13.0)
# Requiere contenedor con acceso a GPU (nvidia-container-toolkit en el host)
cupy-cuda12x[ctk]>=13.0.0

# Visualización
matplotlib>=3.8.0
seaborn>=0.13.0

# Monitoreo de sistema
psutil>=5.9.0
pynvml>=11.5.0

# Exportación y análisis
pyarrow>=15.0.0
tabulate>=0.9.0

# Utilidades
tqdm>=4.66.0
pyyaml>=6.0.1
click>=8.1.7
python-dotenv>=1.0.0

# Calidad de código
scipy>=1.12.0
